import { primaryBrand } from "@/config/brands";

export const siteConfig = {
  name: "Studio OS",
  description: "Digital Operating System for brands",
  primaryBrandName: primaryBrand.name,
} as const;
